git config --global user.nale 'mldn'

git config --global user.email 'mlaoliu@126.com'

md e:\gitpro

git init

git init --bare

git status

git add 文件名称

git commit -m "NEW File Hello.java"

//批量添加文件到版本库中
git add .

进行版本库的提交

git commit -m "add Two Files"

直接将修改后的文件增加到版本库中

git commit -a -m "Change Emp.java File"

比较文件 git diff

git diff Hello.java

远程连接

git remote add origin git@github.com:mlaoliu/hr.git

推送
git push -u origin  master
git push -u origin master

进行提交

git commit -a -m "In Hello.java File Add Two Statement"

查询文件提交时详细时间

git log Hello.java


SSH Key 生成密钥

ssh-keygen -t rsa -C "mlaoliu@126.com"


